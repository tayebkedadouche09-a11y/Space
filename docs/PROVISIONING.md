# Provisioning

## Modes
1. **native** — GITHUB_TOKEN, GITHUB_OWNER, VERCEL_TOKEN, product.sourceRepoUrl
2. **external** — PROVISIONING_API_URL + PROVISIONING_API_KEY
3. **manual** — owner completes delivery in Admin

READY requires instanceUrl + sourceReady + licenseReady + healthOk.
